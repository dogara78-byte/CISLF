source "https://rubygems.org"

# Matches the Jekyll/plugin versions GitHub Pages runs in production.
# `bundle exec jekyll serve` locally will render exactly what GitHub will publish.
gem "github-pages", group: :jekyll_plugins
